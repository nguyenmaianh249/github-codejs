function sum(numbers) {
    var sum =0;
        if (numbers.length >0){
        for ( let i of numbers){
            sum += array[i];
        }
        return sum ;
        }
        else if (numbers.length===0)
          return sum===0;
    }  
    